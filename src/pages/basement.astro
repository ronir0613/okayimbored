---
import BuildingLayout from '../layouts/BuildingLayout.astro';
import TheBasement from '../components/TheBasement';
---

<BuildingLayout title="The Basement" noindex={true} mainClass="">
  <TheBasement client:only="react" />
</BuildingLayout>

<script>
  import { navigate } from 'astro:transitions/client';

  // We want to still be able to navigate back in a hidden way if needed,
  // but The Basement is mostly a dead end or you just use browser back.
  // We'll leave it as a pure dead end as requested "where forgotten ideas patiently wait, even if nobody ever comes back for them."
</script>
